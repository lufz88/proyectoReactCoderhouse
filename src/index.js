import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);

// aqui solo vinculo mi html con mi componente mas grande
// para cada cosa que queramos hacer, creamos un componente (archivo js - comenzando con mayúscula)
